# 语音输入权限插件版解决方案（C# + H5 企业 App 集成）

> **背景**：本网关项目未来两条路——① 维持现在的独立网关服务形态；② 作为平板端侧部署的插件，
> 企业 App 采用 **C#（.NET MAUI）+ H5** 技术栈。本文解决"插件形态下 H5 页面如何拿到麦克风音频"的问题。
>
> **推荐路线：原生采音插件（绕开 WebView）**。原 getUserMedia + WebChromeClient 桥接方案降级为备选/对照，
> 保留在文末（它曾是首版方案，仍有参考价值与适用场景）。

---

## 一、推荐路线：原生采音插件（AudioRecord 直采，不走 WebView）

### 1.1 核心思路

把"拿麦克风"这件事从 Web 层挪到原生层——**WebView 的 getUserMedia 完全不参与**，
权限与采集全部由插件在原生层自主完成：

```
[WebView H5]  ──(不需要 getUserMedia)──✕
     │ JS bridge（addJavascriptInterface / EvaluateJS / Hybrid API）
     ▼
[插件原生层 AudioRecord]  ← RECORD_AUDIO 运行时权限（App 级，无"安全上下文"概念）
     │ 16kHz int16 → float32 转换（每块 ~200ms）
     ▼
[WebSocket] → ASR 网关（或端侧部署时直接进本机 ASR 链）
```

### 1.2 为什么这是权限维度最优解

| 维度 | getUserMedia（Web 层） | **原生 AudioRecord（本方案）** |
|---|---|---|
| 安全上下文要求 | HTTPS / localhost **硬门槛**，HTTP+局域网 IP 直接禁用 | **无**——原生 API 不存在这个概念 |
| WebView 权限桥接 | 必须 `WebChromeClient.OnPermissionRequest`，MAUI 默认静默拒绝 | **不需要** |
| App 级运行时权限 | 经 WebView 桥接间接授予 | 直接 `Permissions.RequestAsync` 一次搞定 |
| 企业 App 改动量 | 桥接层 + 可能的 mixedContent 配置 | **最小：只引插件包** |
| ROM 兼容性 | WebView 实现差异坑（NotReadableError 等） | 走系统 `AudioRecord`，各 ROM 行为一致 |
| iOS | MAUI 缺公开的 WKUIDelegate 麦克风授权回调，是短板 | `AVAudioEngine` 同样绕过，iOS 短板消失 |

**本质**：权限不再依赖 H5 的加载来源。企业 App 的 WebView 可以继续加载任何 `http://` 页面，麦克风照样工作——
"另一台电脑部署后 ca.crt / 404" 那一整类问题从根上消失。

### 1.3 与被搁置的 wesunsoft-pcm-recorder 概要 2.2 的渊源

**本方案不是新发明**。`语音集成方案-概要.md` 第二章 2.2 早已规划过 Cordova 原生录音插件
`wesunsoft-pcm-recorder`：

- 原生 `AudioRecord`（**VOICE_RECOGNITION 音源 / 16kHz / 16bit / 单声道**）
- 200ms 一块，Base64 经 `PluginResult` 流式回传 JS
- 运行时申请 `RECORD_AUDIO`

当年因"WebView getUserMedia 采集能绕开插件开发"而被搁置（见
`语言输入表格说明文档.md` §十二"缺陷与未覆盖项"）。**现在 WebView 路线的权限墙
（安全上下文 + MAUI 桥接缺失）证明了当年概要的前瞻性**——本方案即该概要在
MAUI 语境下的复活与落地，设计参数直接沿用：

| 概要 2.2 设计 | 本方案沿用 | 说明 |
|---|---|---|
| `AudioSource.VOICE_RECOGNITION` | ✅ 保留 | 关闭系统 AGC/降噪，**保留原始频谱**——我们有自己的 GTCRN 降噪，系统预处理反而干扰 |
| 16kHz / 单声道 | ✅ 保留 | 与网关 `AsrProvider:SampleRate=16000` 一致 |
| PCM 16bit | ✅ 保留，**采集后转 float32** | 网关上行协议是 float32（`语音输入表格说明文档.md` §十）；原生层做 int16→float32 转换 |
| 200ms 分块 | ✅ 保留 | 与 voice-mic.js worklet 的 40ms×N 块语义等价，网关无感 |

### 1.4 插件实现要点（C# / MAUI 侧）

```csharp
public class VoiceMicService
{
    AudioRecord? _rec;
    WebSocket? _ws;
    CancellationTokenSource? _cts;

    public async Task StartAsync(string gatewayUrl, string table)
    {
        // 1) App 级运行时权限（一次性；不依赖 WebView）
        var status = await Permissions.CheckStatusAsync<Permissions.Microphone>();
        if (status != PermissionStatus.Granted)
            status = await Permissions.RequestAsync<Permissions.Microphone>();
        if (status != PermissionStatus.Granted) throw new UnauthorizedAccessException("麦克风权限被拒绝");

        // 2) 原生采集：VOICE_RECOGNITION 音源保留原始频谱（配合自有 GTCRN 降噪）
        var minBuf = AudioRecord.GetMinBufferSize(16000, ChannelIn.Mono, Encoding.Pcm16bit);
        _rec = new AudioRecord(AudioSource.VoiceRecognition, 16000, ChannelIn.Mono,
                               Encoding.Pcm16bit, minBuf * 4);
        _rec.StartRecording();

        // 3) WebSocket 连网关（协议与 voice-mic.js 完全一致）
        _ws = new WebSocket($"{gatewayUrl}/api/speech/asr/stream?table={Uri.EscapeDataString(table)}");
        await _ws.ConnectAsync(_cts.Token);

        // 4) 采集线程：int16 → float32 → 分块推送
        _ = Task.Run(async () =>
        {
            var buf = new short[3200]; // 200ms @16kHz
            while (!_cts.IsCancellationRequested)
            {
                var n = _rec.Read(buf, 0, buf.Length);
                var floats = new float[n];
                for (int i = 0; i < n; i++) floats[i] = buf[i] / 32768f;
                // float[] → byte[] → WS 二进制帧（与 voice-mic.js 相同协议）
                await _ws.SendAsync(floats.ToBytes(), WebSocketMessageType.Binary, true, _cts.Token);
            }
        });
    }

    public void Stop() { /* 发 {"type":"stop"} + 等 cells + 释放 AudioRecord */ }
}
```

### 1.5 H5 侧零改动

**`voice-mic.js` 的协议（partial / final / cells / stop 语义）原样保留**——只是 PCM 来源从
"AudioWorklet 采集"换成"原生桥推送"。`index.html` 的 `mic.toggle()` 按钮语义可以继续用，
触发方式改为调 JS 桥。企业 App 的 H5 若自己实现 UI，只需按 `api文档.md` 的 WS 协议收发。

### 1.6 两条路的适配

| 将来的路 | 本方案适配 |
|---|---|
| **路 1：维持网关服务形态** | 插件在平板原生采音 → PCM 经 WS 推给局域网网关。网关零改动（协议不变），HTTP/HTTPS 之争消失 |
| **路 2：平板端侧部署**（见 `安卓化方案.md`） | 同样受益：ASR 本机跑时 PCM 从原生层直接进 sherpa/降噪链，连 WS 都省；Web 层完全不出场 |

---

## 二、备选/对照路线：getUserMedia + 自定义 WebChromeClient（原首版方案）

> **保留价值**：MAUI 环境下打通 H5 麦克风权限的官方标准路径，微软工程师确认过；
> 当 H5 页面**必须**用标准 Web 音频 API（如未来要做回声消除/浏览器侧混音）时仍是唯一选择。
> **注意**：本方案只解决权限桥接这一半，**安全上下文那一半它不管**——两道锁必须同时开。

### 2.1 为什么必须自定义 WebChromeClient

MAUI 默认的 `MauiWebChromeClient` 会直接忽略网页的权限请求——H5 调
`getUserMedia({ audio: true })` 会被 WebView 静默拒绝。唯一解法是自定义并替换它。
（对比：cordova-android 10+ 的 `SystemWebChromeClient` 自动做了桥接，这就是当年
cordova 壳"能用"而 MAUI 企业 App"不能用"的差异根源。）

### 2.2 完整实现步骤

**1. AndroidManifest.xml 声明权限**

```xml
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
```

`RECORD_AUDIO` 是录音核心权限；`MODIFY_AUDIO_SETTINGS` 确保可调整音频设置，
部分 Android 版本 WebView 用麦克风必需。

**2. 加载 WebView 前动态申请应用级权限**（建议在页面 OnAppearing 或 WebView 加载前调用）：

```csharp
private async Task RequestMicrophonePermission()
{
    PermissionStatus status = await Permissions.CheckStatusAsync<Permissions.Microphone>();
    if (status != PermissionStatus.Granted)
        await Permissions.RequestAsync<Permissions.Microphone>();
}
```

**3. 自定义 WebChromeClient，重写 OnPermissionRequest**：

```csharp
using Android.Webkit;
using Microsoft.Maui.Handlers;

namespace YourAppName.Platforms.Android;

internal class MyWebChromeClient : MauiWebChromeClient
{
    public MyWebChromeClient(IWebViewHandler handler) : base(handler) { }

    public override void OnPermissionRequest(PermissionRequest request)
    {
        foreach (var resource in request.GetResources())
        {
            if (resource.Equals(PermissionRequest.ResourceAudioCapture, StringComparison.OrdinalIgnoreCase))
            {
                // 应用级权限已在步骤 2 预申请就绪——直接授权，避免在 UI 线程同步查权限
                request.Grant(request.GetResources());
                return;
            }
        }
        base.OnPermissionRequest(request);
    }
}
```

> ⚠️ **原首版方案的 bug 修正**：原文在回调里写
> `Permissions.CheckStatusAsync<...>().Result`——`OnPermissionRequest` 运行在 UI 线程，
> `.Result` 同步阻塞有**死锁风险**。正确做法是按步骤 2 的设计意图：
> **进入页面前预申请，回调里直接 `Grant()`**，回调内不再查权限。

**4. 在 Handler 中注册自定义客户端**（页面 OnHandlerChanged / Loaded 里）：

```csharp
protected override void OnHandlerChanged()
{
    base.OnHandlerChanged();
    if (webView.Handler is IWebViewHandler handler)
    {
        var chromeClient = new MyWebChromeClient(handler);
        var platformWebView = (Android.Webkit.WebView)handler.PlatformView;
        platformWebView.SetWebChromeClient(chromeClient);
    }
}
```

### 2.3 本方案的前置条件：安全上下文（易漏！）

`getUserMedia` 有**两道锁**，桥接只开了第二道：

| 锁 | 谁管 | 说明 |
|---|---|---|
| **安全上下文** | H5 页面的来源 | HTTPS / `http://localhost` / WebViewAssetLoader 的 https 域；`http://<局域网IP>` 下 `navigator.mediaDevices` 直接是 `undefined`，走不到权限回调 |
| WebView 权限桥接 | 宿主 App | 本文 2.2 的内容 |

**若 H5 从网关加载，必须先满足安全上下文**，二选一：

1. **网关走 HTTPS**：`make-cert.bat` 生成自签 CA，平板装 `ca.crt`（详见部署文档 §五），
   H5 从 `https://<网关IP>:15433/` 加载；
2. **H5 打进 App 本地**：assets 打包 + `androidx.webkit.WebViewAssetLoader` 以 `https://<app域>` 提供
   （Android WebView 明确把 WebViewAssetLoader 的 https 域视为安全上下文）。

**只按 2.2 做桥接、H5 仍从 `http://IP` 加载 → 不可行**。

### 2.4 常见问题

| 现象 | 可能原因 | 解决 |
|---|---|---|
| 权限请求被静默拒绝 | 未替换默认 MauiWebChromeClient | 确认 Handler 中已 `SetWebChromeClient` |
| 授权后仍无法录音 | `MODIFY_AUDIO_SETTINGS` 缺失 | AndroidManifest 同时声明两个权限 |
| 应用级权限未就绪 | 加载 H5 前未申请 | WebView 加载前 `Permissions.RequestAsync` |
| iOS 无效 | 只处理了 Android | iOS 需处理 WKUIDelegate 的媒体授权回调，MAUI 当前无公开 API——**这是备选路线在 iOS 的硬短板，推荐路线（原生采音）不受此限** |
| `navigator.mediaDevices` 为 undefined | H5 来源非安全上下文 | 见 2.3，先解决 HTTPS |

---

## 三、两条路线的决策表

| 场景 | 推荐路线 |
|---|---|
| 企业 App 想以**最小集成成本**接入语音填表 | **原生采音插件**（1.4 的 ~100 行 C# + 引用即可） |
| 企业 App 的 H5 需要标准 Web 音频 API（AEC/浏览器侧处理） | 备选路线（2.2）+ 安全上下文（2.3） |
| 平板含 iOS 设备 | **必须原生采音**（备选路线 iOS 短板无解） |
| 端侧部署（ASR 进平板） | **原生采音**（PCM 直进本机 ASR，Web 层不出场） |
| 仅内部测试 / 快速验证 | 现有 cordova 壳 / HTTPS 浏览器直连即可，无需插件 |

---

## 四、参考资源

- 官方文档：.NET MAUI WebView 权限处理
- 社区示例：MauiBlazorPermissionsExample（PermissionManagingBlazorWebChromeClient 完整实现）
- 本项目关联文档：
  - `语音集成方案-概要.md` §2.2（wesunsoft-pcm-recorder 原始设计，本方案推荐路线的渊源）
  - `语言输入表格说明文档.md` §十二（当年搁置原生插件的原因与 WebView 替代路径）
  - `部署文档.md` §五（HTTPS 证书方案——备选路线安全上下文的落地）
  - `安卓化方案.md`（路 2 端侧部署的完整设计）
  - `api文档.md`（WS 协议：partial/final/cells/stop——两路线共用的传输层）
