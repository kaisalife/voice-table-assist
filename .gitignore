# 模型与运行时资产（体积大，不入库）
models/

# VC++ Redistributable 安装包（第三方二进制，随部署包带，不入库）
vc_redist.x64.exe

# 构建产物
bin/
obj/
publish/

# 本地环境变量（含密钥）
.env

# 本地 HTTPS 证书（自签、绑定 IP；用 make-cert.bat 重新生成，私钥不入库）
certs/

# 日志
*.log
