# 复制本文件为 app/.env（或 .env）可覆盖 appsettings.json 中的默认值（环境变量优先，本文件次之）。
# 正式部署推荐直接修改 appsettings.json，或设置系统环境变量。真实密钥不得提交到代码仓库。

# ---- ASR（sherpa 本地离线）----
AsrProvider__Endpoint=ws://127.0.0.1:6006
AsrProvider__SampleRate=16000

# ---- 模型目录覆盖（默认相对可执行文件解析 models/，见 Program.cs）----
# RANER_MODEL_DIR=D:\models\raner-pack