# 复制本文件为 app/.env（或 .env）可覆盖 appsettings.json 中的默认值（环境变量优先，本文件次之）。
# 正式部署推荐直接修改 appsettings.json，或设置系统环境变量。真实密钥不得提交到代码仓库。

# ---- ASR（sherpa-onnx 进程内，P/Invoke）----
# 模型/原生库/端点/热词等配置项见 appsettings.json 的 SherpaServer 段：
#   SherpaServer__ModelingUnit=cjkchar      # 与 tokens.txt 匹配；本仓库模型无 bpe.model，勿设 bbpe
#   SherpaServer__HotwordsScore=2.0
#   SherpaServer__EnableEndpoint=true
# 热词不再走文件：每通会话按表（CreateStream(hotwords)）传入，导入/切表无需重启。

# ---- 模型目录覆盖（默认相对可执行文件解析 models/，见 Program.cs）----
# RANER_MODEL_DIR=D:\models\raner-pack